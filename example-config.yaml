excel:
  - path: "/Users/xxxx/aaa.xlsx"
    sheet: "Sheet 1"
    columns:
      - name: "Key"
        type: int
jira:
  # JIRA API might be multiple for visualizing multi projects
  - endpoint: "xxx"
    project: "xxx"
    auth:
      user: xxx
      password: yyyy
    import-from: "1M"
